name: ${project.name}
main: ${project.groupId}.${project.artifactId}.${project.name}
authors: [timvisee, sgdc3, xephi, Metonymia]
version: ${project.version}
load: startup
softdepend:
    - PermissionsEx
    - PermissionsBukkit
    - bPermissions
    - EssentialsGroupManager
    - zPermissions
    - Vault
    - Permissions
    - Multiverse-Core
    - AuthMe
    - AuthMeReloaded
commands:
  dm:
    description: Dungeon Maze command
    usage: /dm
  dungeonmaze:
    description: Dungeon Maze command
    usage: /dungeonmaze
